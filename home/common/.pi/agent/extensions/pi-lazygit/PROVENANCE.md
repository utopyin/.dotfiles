# pi-lazygit provenance

- Upstream: <https://github.com/Neil-urk12/pi-dots>
- Imported release: `npm:@neilurk12/pi-lazygit@0.1.1`
- Imported: 2026-08-25
- Local change: replaced the conflicting `Ctrl+Shift+G` shortcut with `Alt+L`
  (`Option+L` on macOS) and restricted fullscreen takeover to TUI mode.

To refresh, inspect the latest upstream npm release, update `index.ts` while
preserving the local shortcut and TUI guard, then update the release and import
date above.
